## REMOVED Requirements

### Requirement: Grid top padding via empty string
**Reason**: The dashboard widget that this spec supported is being removed entirely. The grid top padding was a visual spacing feature for the widget above the editor. With the widget gone, this spec has no purpose and no component to apply to.
**Migration**: No migration needed. The widget removal in spec-teams-extension eliminates the only consumer of this padding behavior.