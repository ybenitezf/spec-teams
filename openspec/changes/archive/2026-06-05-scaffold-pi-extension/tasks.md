# Tasks: Scaffold Pi Extension

## Implementation

- [x] 1. **Create `package.json`** with Pi package metadata, peer dependencies, and `pi` manifest
- [x] 2. **Create `tsconfig.json`** with TypeScript config for IDE support
- [x] 3. **Create `extensions/index.ts`** — minimal extension skeleton
- [x] 4. **Update `openspec/config.yaml`** with project context (tech stack, conventions)

## Verification

- [x] 5. **Verify extension loads**: run `pi -e ./extensions/index.ts -p "hello"` from project root, confirm no errors
