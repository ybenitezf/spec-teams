{
  "name": "spec-teams-extension",
  "version": "0.1.0",
  "description": "A Pi coding agent extension for spec-driven team workflows",
  "license": "MIT",
  "keywords": ["pi-package"],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3"
  },
  "pi": {
    "extensions": ["./extensions"]
  }
}
